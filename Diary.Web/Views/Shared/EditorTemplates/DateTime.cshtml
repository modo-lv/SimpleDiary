@using Simpler.Net.Mvc
@using Simpler.Net.Time
@model DateTime?

@(Html.SimplerTextBoxFor(model => model)
	.SetAttribute("data-provide", "datetimepicker")
	.SetAttribute("value", Model.ToShortDateString())
	)