@using Simpler.Net.Mvc
@using Simpler.Net.Time
@model DateTime?

@(Html.SimplerTextBoxFor(model => model)
	.SetAttribute("value", Model?.ToShortDateTimeString())
	.SetAttribute("data-type", "datetime")
)